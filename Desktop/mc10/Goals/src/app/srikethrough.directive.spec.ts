import { SrikethroughDirective } from './srikethrough.directive';

describe('SrikethroughDirective', () => {
  it('should create an instance', () => {
    const directive = new SrikethroughDirective();
    expect(directive).toBeTruthy();
  });
});
